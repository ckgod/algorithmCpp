#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int n, m;
int useMoney[1001];


int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	cin >> n >> m;
	for (int i = 0; i < n; i++) {
		cin >> useMoney[i];
	}



	return 0;
}