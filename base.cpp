#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

int n;
bool arr[1001];

void init() {
	for (int i = 1; i <= 1000; i++) {
		if (i < 100) arr[i] = true;
		else if (i < 1000) {
			int f, s, t;
			f = i / 100;
		}
	}
}


int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	cin >> n;


	return 0;
}